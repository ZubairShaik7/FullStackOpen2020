client->browser:

note left of browser
    user creates new note
    and Saves it
end note

browser->server: HTTP POST http://fullstack-exampleapp.herokuapp.com/new_note_spa

note left of server
    server then receives the client request
    to write a note to database
end note

note right of browser
    server then writes the note to database
    and the browser stays on same page
end note

server->browser: status code "201 Created"
